# 灵动陪伴 - 在线构建APK指南

由于您目前没有Android开发环境，这里提供几种在线构建APK的方案：

## 🌟 推荐方案

### 1. GitHub Actions (免费)
**优点**: 完全免费，自动化构建
**步骤**:
1. 将项目上传到GitHub仓库
2. 创建 `.github/workflows/build.yml` 文件
3. 推送代码后自动构建APK
4. 从Actions页面下载构建好的APK

### 2. Expo EAS Build (推荐)
**优点**: 专业的React Native构建服务
**步骤**:
1. 注册Expo账号 (免费)
2. 安装EAS CLI: `npm install -g @expo/eas-cli`
3. 登录: `eas login`
4. 构建: `eas build --platform android`
5. 下载APK文件

### 3. AppCenter (微软)
**优点**: 企业级构建服务，有免费额度
**步骤**:
1. 注册Microsoft AppCenter账号
2. 连接GitHub仓库
3. 配置构建设置
4. 自动构建并分发APK

## 🚀 快速方案 - 使用GitHub Actions

我为您准备了GitHub Actions配置文件：

### 创建构建工作流
在项目根目录创建 `.github/workflows/build-android.yml`:

\`\`\`yaml
name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v2
      
    - name: Install dependencies
      run: npm install
      
    - name: Initialize React Native project
      run: |
        npx react-native init TempProject --skip-install
        cp -r TempProject/android .
        rm -rf TempProject
        
    - name: Build APK
      run: |
        cd android
        ./gradlew assembleRelease
        
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: 灵动陪伴-APK
        path: android/app/build/outputs/apk/release/app-release.apk
\`\`\`

### 使用步骤:
1. 在GitHub创建新仓库
2. 上传您的项目代码
3. 创建上述workflow文件
4. 推送代码，GitHub会自动构建
5. 在Actions页面下载APK

## 💡 本地虚拟环境方案

### 使用Docker构建
如果您有Docker环境，可以使用Android构建容器：

\`\`\`bash
# 拉取Android构建镜像
docker pull reactnativecommunity/react-native-android

# 运行构建容器
docker run --rm -v $(pwd):/workspace -w /workspace reactnativecommunity/react-native-android bash -c "
  npm install &&
  npx react-native init TempProject --skip-install &&
  cp -r TempProject/android . &&
  rm -rf TempProject &&
  cd android &&
  ./gradlew assembleRelease
"
```

## 🔧 云端开发环境

### 1. GitHub Codespaces
- 直接在浏览器中开发
- 预装Android开发环境
- 可以直接构建APK

### 2. Gitpod
- 免费的云端IDE
- 支持Android开发
- 一键启动开发环境

### 3. Replit
- 在线编程平台
- 支持React Native项目
- 可以构建移动应用

## 📱 最简单方案 - 使用现成服务

### 1. Appetize.io
- 在线Android模拟器
- 可以测试APK
- 无需本地环境

### 2. BrowserStack
- 真机测试平台
- 支持APK上传测试
- 有免费试用

## 🎯 推荐流程

**最简单的方式**:
1. 将项目上传到GitHub
2. 使用GitHub Actions自动构建
3. 下载构建好的APK文件
4. 安装到Android设备测试

**如果需要立即测试**:
1. 使用Expo EAS Build
2. 几分钟内获得APK
3. 扫码直接安装到手机

## 📞 技术支持

如果您需要帮助设置任何一种构建方案，我可以：
1. 提供详细的配置文件
2. 协助解决构建问题
3. 优化构建流程

---

**建议**: 使用GitHub Actions是最经济实惠的方案，完全免费且自动化程度高。