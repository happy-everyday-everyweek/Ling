name: 构建灵动陪伴APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 设置Node.js环境
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 设置Java环境
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: 设置Android SDK
      uses: android-actions/setup-android@v3
      
    - name: 安装依赖
      run: npm install
      
    - name: 运行构建脚本
      run: node build-simple-apk.js
      
    - name: 上传APK文件
      uses: actions/upload-artifact@v4
      with:
        name: 灵动陪伴-APK
        path: ./灵动陪伴-v*.apk
        retention-days: 30
        
    - name: 创建Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: 灵动陪伴 v1.0.${{ github.run_number }}
        body: |
          🎉 灵动陪伴 - AI情感陪伴应用
          
          ## 功能特性
          - 🤖 智能AI对话陪伴
          - 📝 日记记录与管理
          - 💭 心情分析与统计
          - 📱 社交帖子互动
          
          ## 安装说明
          1. 下载APK文件
          2. 在Android设备上启用"未知来源"安装
          3. 点击APK文件进行安装
          4. 首次使用需要配置DeepSeek API密钥
          
          ## 更新内容
          - 自动构建版本 v1.0.${{ github.run_number }}
          - 优化用户体验
          - 修复已知问题
        files: ./灵动陪伴-v*.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}