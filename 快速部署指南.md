# 🚀 灵动陪伴 - 快速部署指南

## 📱 立即获取APK的方法

### 方法一：GitHub Actions (推荐，完全免费)

#### 步骤：
1. **创建GitHub仓库**
   - 登录GitHub.com
   - 点击"New repository"
   - 仓库名：`ai-companion-app`
   - 设为Public（免费使用Actions）

2. **上传项目代码**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/你的用户名/ai-companion-app.git
   git push -u origin main
   ```

3. **自动构建**
   - 代码推送后，GitHub Actions会自动开始构建
   - 访问仓库的"Actions"标签页查看进度
   - 构建完成后，点击构建任务
   - 在"Artifacts"部分下载APK文件

4. **获取APK**
   - 下载名为"灵动陪伴-APK"的文件
   - 解压得到`灵动陪伴-v1.0.0.apk`

### 方法二：在线IDE构建

#### 使用Gitpod (免费50小时/月)
1. 访问：`https://gitpod.io/#https://github.com/你的用户名/ai-companion-app`
2. 等待环境启动
3. 在终端运行：
   ```bash
   npm install
   chmod +x build.sh
   ./build.sh
   ```
4. 下载生成的APK文件

#### 使用GitHub Codespaces
1. 在GitHub仓库页面点击"Code" > "Codespaces"
2. 创建新的Codespace
3. 运行构建命令
4. 下载APK文件

### 方法三：Docker构建 (如果有Docker环境)

```bash
# 创建Dockerfile
cat > Dockerfile << 'EOF'
FROM reactnativecommunity/react-native-android

WORKDIR /app
COPY . .

RUN npm install
RUN npx react-native init TempProject --skip-install && \
    cp -r TempProject/android . && \
    rm -rf TempProject

RUN cd android && ./gradlew assembleRelease

CMD ["cp", "android/app/build/outputs/apk/release/app-release.apk", "/output/灵动陪伴-v1.0.0.apk"]
EOF

# 构建镜像并运行
docker build -t ai-companion-builder .
docker run --rm -v $(pwd)/output:/output ai-companion-builder
```

## 📋 构建状态检查

### GitHub Actions构建日志
- 绿色✅：构建成功
- 红色❌：构建失败，点击查看错误日志
- 黄色🟡：构建进行中

### 常见问题解决
1. **构建失败**：检查代码语法错误
2. **依赖问题**：确保package.json正确
3. **权限问题**：确保仓库为Public

## 🎯 最快方案总结

**如果您有GitHub账号**：
1. 创建仓库 (2分钟)
2. 上传代码 (3分钟)
3. 等待构建 (10-15分钟)
4. 下载APK (1分钟)

**总耗时：约20分钟即可获得APK**

## 📱 APK安装指南

### Android设备安装
1. 将APK文件传输到手机
2. 在手机上找到APK文件
3. 点击安装（可能需要允许"未知来源"）
4. 安装完成后打开应用
5. 首次使用需要输入DeepSeek API密钥

### 获取DeepSeek API密钥
1. 访问：https://platform.deepseek.com/
2. 注册账号（支持邮箱/手机号）
3. 进入API密钥页面
4. 创建新密钥
5. 复制密钥到应用中

## 🔧 技术支持

如果遇到任何问题：
1. 检查GitHub Actions构建日志
2. 确认所有文件都已正确上传
3. 验证package.json配置
4. 查看构建错误信息

---

**推荐使用GitHub Actions方案，完全免费且自动化程度最高！**